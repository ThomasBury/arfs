version: 2

python:
   version: 3.8
   install:
      - method: pip
        path: .
        requirements: 
            - docs/requirements.txt
        extra_requirements:
            - docs
   system_packages: true